# FormulaDebugger

```bash
$ dotnet restore

$ dotnet build Debugger.sln /p:Configuration=Debug|Release /p:Platform=x64

$ ./bin/<Configuration>/<OS>/<Arch>/net6.0/Debugger 
  or
$ dotnet ./bin/<Configuration>/<OS>/<Arch>/net6.0/Debugger.dll
```